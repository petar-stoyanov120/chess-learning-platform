// P3.6 — Weekly digest email to parents summarising their child's lesson and
// puzzle activity for the previous week. Fill in once the Parent role and the
// parent↔student membership model are added.
import { logger } from '../config/logger';

export async function sendParentDigest(): Promise<void> {
  logger.debug('parentDigest job: not yet implemented (P3.6)');
}
