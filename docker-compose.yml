version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: chess
      POSTGRES_PASSWORD: chess_secret
      POSTGRES_DB: chess_learn
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
